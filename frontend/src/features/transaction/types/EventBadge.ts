/** Represents a styled inline badge for displaying event payload data in the timeline. */
export interface EventBadge {
  /** Text label displayed inside the badge. */
  label: string;
  /** Tailwind class string applied to the badge element for colorization. */
  colorClass: string;
}
